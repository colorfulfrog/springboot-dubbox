<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yxhl.stationbiz.system.provider.dao.schedule.ScheduleBusStowageDao">
	
	<select id="selPageList" resultType="com.yxhl.stationbiz.system.domain.entity.schedule.ScheduleBusStowage">
		select * from bs_schedule_bus_stowage
		<where>
			<!-- 条件 -->
		</where>
		order by create_time desc
	</select>
</mapper>