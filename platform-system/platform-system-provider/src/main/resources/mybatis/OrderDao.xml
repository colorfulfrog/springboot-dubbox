<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yxhl.stationbiz.system.provider.dao.ticket.OrderDao">
	
	<select id="selPageList" resultType="com.yxhl.stationbiz.system.domain.entity.ticket.Order">
		select * from bs_order
		<where>
			<!-- 条件 -->
		</where>
		order by create_time desc
	</select>
</mapper>